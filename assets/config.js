/* ============================================================
   Hope Homeo Clinic — the only file you need to edit to make
   appointment notifications work. Nothing else references keys.
   ============================================================ */
window.HHC_CONFIG = {

  /* 1. EMAIL  — bookings arrive in your Gmail inbox.
        Get a free key at https://web3forms.com (enter
        hopehomeoclinic15@gmail.com, they email you the key),
        then paste it between the quotes below.               */
  web3formsKey: "",

  /* 2. GOOGLE SHEET — optional. Every booking is also added as
        a row in a spreadsheet you own. Follow SETUP.md step 3,
        then paste the /exec web-app URL between the quotes.   */
  sheetEndpoint: "",

  /* 3. Clinic contact — used by the WhatsApp and call buttons. */
  whatsapp: "917481908030",
  phone: "+917481908030",
  email: "hopehomeoclinic15@gmail.com",
};
